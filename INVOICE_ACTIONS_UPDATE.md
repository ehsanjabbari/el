# به‌روزرسانی جدید: دکمه‌های ویرایش و حذف فاکتورهای ورودی

## ✨ ویژگی‌های جدید اضافه شده:

### 1. دکمه‌های عملیات در فاکتورهای ورودی
- **دکمه ویرایش** (آیکون قلم): برای تغییر محتوای فاکتور
- **دکمه حذف** (آیکون سطل زباله): برای حذف فاکتور از سیستم

### 2. ویرایش کامل فاکتور
- تغییر تاریخ فاکتور
- ویرایش محصولات موجود در فاکتور
- افزودن محصولات جدید به فاکتور
- حذف محصولات از فاکتور
- تغییر تعداد و قیمت واحد هر محصول
- ویرایش توضیحات

### 3. حذف امن فاکتور
- تأیید قبل از حذف (پیام هشدار)
- به‌روزرسانی خودکار موجودی انبار پس از حذف
- حذف کامل از localStorage

### 4. دکمه‌های عملیات در مدال جزئیات
- دکمه‌های ویرایش و حذف در مدال جزئیات فاکتور
- دسترسی آسان به عملیات

## 🎨 تغییرات ظاهری:

### استایل‌های CSS جدید:
```css
/* دکمه‌های آیکون */
.btn-icon {
    background-color: var(--surface-bg);
    color: var(--text-primary);
    border: 1px solid var(--border-subtle);
    padding: var(--space-xs);
    border-radius: var(--radius-sm);
    cursor: pointer;
    transition: all 0.2s ease;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-size: 14px;
    width: 32px;
    height: 32px;
}

/* دکمه‌های خطرناک (حذف) */
.btn-icon.btn-danger {
    color: var(--error);
    border-color: var(--error);
}

.btn-icon.btn-danger:hover {
    background-color: var(--error);
    color: white;
}

/* گروه‌بندی عملیات */
.item-actions {
    display: flex;
    align-items: center;
    gap: var(--space-xs);
}
```

## 🔧 متدهای جاوااسکریپت جدید:

### 1. `editInputInvoice(invoiceId)`
- باز کردن مدال ویرایش فاکتور
- نمایش فرم با اطلاعات فعلی فاکتور
- امکان ویرایش تاریخ و محصولات

### 2. `handleEditInputInvoiceSubmit(e, invoiceId)`
- پردازش فرم ویرایش
- محاسبه مجموع جدید فاکتور
- به‌روزرسانی فاکتور در localStorage
- به‌روزرسانی انبار و فهرست فاکتورها

### 3. `deleteInputInvoice(invoiceId)`
- نمایش پیام تأیید
- حذف فاکتور از داده‌ها
- به‌روزرسانی انبار و فهرست

### 4. `removeInvoiceItem(button, index)`
- حذف محصول از فرم ویرایش
- حذف DOM element

### 5. `addInvoiceItem()`
- افزودن محصول جدید به فرم ویرایش
- ایجاد فرم دینامیک

## 📱 نحوه استفاده:

### 1. ویرایش فاکتور:
1. به تب **فاکتورهای ورودی** بروید
2. روی دکمه **ویرایش** (آیکون قلم) کلیک کنید
3. در مدال باز شده، اطلاعات مورد نیاز را تغییر دهید
4. روی **ذخیره تغییرات** کلیک کنید

### 2. حذف فاکتور:
1. روی دکمه **حذف** (آیکون سطل زباله) کلیک کنید
2. در پیام تأیید، **OK** را انتخاب کنید
3. فاکتور حذف شده و انبار به‌روزرسانی می‌شود

### 3. از طریق جزئیات فاکتور:
1. روی عنوان یا تاریخ فاکتور کلیک کنید
2. در مدال جزئیات، دکمه‌های ویرایش/حذف را ببینید
3. عملیات مورد نظر را انجام دهید

## ⚠️ نکات مهم:
- هنگام ویرایش، تمام فیلدها باید پر شوند
- پس از حذف فاکتور، موجودی انبار به‌روزرسانی می‌شود
- امکان بازیابی فاکتور حذف شده وجود ندارد

## 🔄 فایل‌های به‌روزرسانی شده:
- `app.js` - کد اصلی جاوااسکریپت
- `styles.css` - استایل‌های جدید
- `index.html` - فایل اصلی سیستم

## 🧪 فایل‌های تست:
- `invoice_actions_test.html` - تست عملکرد دکمه‌ها
- `edit_button_test.html` - تست دکمه ویرایش محصولات

توسعه‌دهنده: MiniMax Agent